{
  "$id": "user.events.user.created.v1",
  "title": "UserCreatedEventV1",
  "type": "object",
  "properties": {
    "eventId":        { "type": "string", "format": "uuid" },
    "eventType":      { "type": "string" },
    "eventVersion":   { "type": "integer" },
    "schemaVersion":  { "type": "string" },
    "userId":         { "type": "string" },
    "name":           { "type": "string" },
    "email":          { "type": "string", "format": "email" },
    "occurredAt":     { "type": "string", "format": "date-time" }
  },
  "required": ["eventId","eventType","eventVersion","schemaVersion","userId","name","email","occurredAt"]
}
